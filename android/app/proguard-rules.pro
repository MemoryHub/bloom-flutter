# Flutter plugins and WorkManager ship their own consumer rules. Keep this file
# explicit so release resource/code shrinking stays enabled and project-specific
# exceptions can be added here if a future plugin requires one.
